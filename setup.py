from setuptools import setup

# this is going to allow us to install the package in editable mode.
# if we change the src/doc2img package we don't need to re-install the package
if __name__ == "__main__":
    setup()